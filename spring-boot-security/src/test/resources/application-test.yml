server:
  port: 8080

#datasource
spring:

  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/sample_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: tester
    password: pwd1234
    driver-class-name: org.mariadb.jdbc.Driver

    hikari:
      maximum-pool-size: 5
#      driver-class-name: org.h2.Driver
#      jdbc-url: jdbc:h2:mem:junit;MODE=Mysql;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#      username: sa
#      password: test1234
#      schema: classpath*:db/schema.sql

  jpa:
    hibernate:
#      ddl-auto: create # 개발에만 사용!!!!!!  
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    show-sql: true
    generate-ddl: true
    
#log
logging:
  level:
    root: INFO
    '[org.hibernate.type.descriptor.sql]': trace
        